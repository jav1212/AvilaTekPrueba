import { Montserrat, Mulish, Work_Sans } from 'next/font/google';

export const MONTSERRAT = Montserrat({
  subsets: ['latin'],
});

export const MULISH = Mulish({ subsets: ['latin'] });

export const WORK_SANS = Work_Sans({ subsets: ['latin'] });
